# @nbins/api

Cloudflare Workers-compatible Hono skeleton for NBINS.

Available routes:

- `GET /health`
- `GET /api/meta`

Local development is expected to use `wrangler dev` after workspace dependencies are installed.

