import React from 'react';

export function Reports() {
  return (
    <main className="workspace">
      <section className="hero" style={{ paddingBottom: 0 }}>
        <div>
          <p className="eyebrow">NBINS CORE MODULE</p>
          <h2>DATA WAREHOUSE & REPORTS</h2>
        </div>
      </section>
      <div style={{ marginTop: '24px', padding: '32px', textAlign: 'center', color: 'var(--nb-text-muted)', border: '1px dashed var(--nb-border)', borderRadius: '16px' }}>
         The Reports module is awaiting implementation...
      </div>
    </main>
  );
}
