/**
 * @deprecated This monolithic file has been split into the `api/` directory.
 * It is kept temporarily to avoid breaking existing imports.
 * Please import directly from `../api` or specific modules like `../api/ncrs` in the future.
 */
export * from "./api/index";
