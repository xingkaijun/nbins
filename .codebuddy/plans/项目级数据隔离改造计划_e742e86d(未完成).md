---
name: 项目级数据隔离改造计划
overview: 实现在Projects页面选择项目后，Dashboard、Observations、Ncrs、Reports页面仅显示该项目的数据，实现项目级别的数据隔离和权限控制
todos:
  - id: explore-codebase
    content: 使用 [subagent:code-explorer] 探索项目上下文实现模式、API过滤参数支持情况
    status: pending
  - id: create-project-context
    content: 创建 ProjectContext 提供全局项目状态管理
    status: pending
    dependencies:
      - explore-codebase
  - id: modify-projects-page
    content: 修改 Projects.tsx，点击项目时设置全局状态并跳转
    status: pending
    dependencies:
      - create-project-context
  - id: extend-api-functions
    content: 扩展前端 API 函数支持 projectId 参数过滤
    status: pending
    dependencies:
      - explore-codebase
  - id: modify-backend-inspections
    content: 后端 inspections 路由增加 projectId 过滤逻辑
    status: pending
    dependencies:
      - extend-api-functions
  - id: modify-dashboard
    content: Dashboard.tsx 根据选中项目过滤显示数据
    status: pending
    dependencies:
      - modify-backend-inspections
  - id: modify-ncrs
    content: Ncrs.tsx 根据选中项目加载船只和NCR数据
    status: pending
    dependencies:
      - create-project-context
  - id: modify-reports
    content: Reports.tsx 根据选中项目过滤统计数据
    status: pending
    dependencies:
      - modify-backend-inspections
  - id: sync-observations
    content: Observations.tsx 与全局项目状态同步
    status: pending
    dependencies:
      - create-project-context
---

## 需求概述

当前系统存在权限隔离问题：admin用户在Hall页面选择项目后，Dashboard、Observations、NCRs、Reports页面仍然显示所有项目的船只数据，导致数据量过大且权限隔离不佳。

## 核心功能需求

1. **项目上下文状态管理**：创建全局状态保存当前选中的项目
2. **Hall页面改造**：点击项目卡片时，将项目ID存储到全局状态，并跳转到对应页面
3. **Dashboard页面过滤**：只显示当前选中项目下的船只检查项
4. **Observations页面**：已有项目选择功能，需与全局状态同步
5. **NCRs页面过滤**：根据选中项目加载船只和NCR数据
6. **Reports页面过滤**：统计和图表只显示当前项目数据

## 技术方案

### 架构设计

使用 **React Context + URL参数** 双重机制：

- **Context**：全局状态管理，保存当前选中项目
- **URL参数**：支持页面刷新后状态保持，支持分享链接

```mermaid
flowchart TD
    A[Projects.tsx Hall页面] -->|选择项目| B[ProjectContext]
    B -->|projectId| C[Dashboard.tsx]
    B -->|projectId| D[Observations.tsx]
    B -->|projectId| E[Ncrs.tsx]
    B -->|projectId| F[Reports.tsx]
    
    C -->|fetchInspectionList projectId| G[API /inspections?projectId=xxx]
    D -->|fetchShips projectId| H[API /ships?projectId=xxx]
    E -->|fetchNcrs shipId| I[API /ncrs/ships/:shipId]
    F -->|fetchInspectionList projectId| G
```

### 实现要点

1. **创建 ProjectContext**

- 存储 `selectedProjectId`
- 提供 `setSelectedProject` 方法
- 从 URL 参数初始化状态

2. **修改 API 层**

- `fetchInspectionList()` 增加 `projectId` 可选参数
- 后端 `/inspections` 路由增加 `projectId` 查询参数过滤

3. **各页面改造**

- Dashboard：根据 projectId 过滤船只列表，只显示该项目下的检查项
- NCRs：根据 projectId 加载船只列表，再加载对应NCR
- Reports：统计图表只计算当前项目数据
- Observations：与全局状态双向同步

### 数据流

```
用户选择项目 → ProjectContext 更新 → URL 更新 → 各页面监听 → API请求带projectId → 后端过滤返回
```

### 性能考虑

- 使用 `useMemo` 缓存过滤结果
- 项目切换时清除之前的数据，避免闪烁
- API请求使用 `projectId` 参数，减少传输数据量

## Agent Extensions

### SubAgent

- **code-explorer**
- Purpose: 深入探索项目结构，确认API路由实现细节、权限过滤逻辑、以及现有Context模式
- Expected outcome: 获取准确的代码结构信息，确保方案与现有架构一致��方案与现有架构一致